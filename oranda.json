{
  "dist_dir": "./docs",
  "path_prefix": "twt",
  "artifacts": {
    "package_managers": {
      "crates.io": "cargo install twt --locked --profile=dist",
      "binstall": "cargo binstall twt",
      "nix flake": "nix profile install github:msfjarvis/twt"
    }
  }
}
